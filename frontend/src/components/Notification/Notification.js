import React from 'react';
import { useNotification } from '../../contexts/NotificationContext';
import './Notification.css';

const Notification = () => {
  // This component is now redundant since we've moved the notification display
  // to the NotificationContext provider itself
  return null;
};

export default Notification; 